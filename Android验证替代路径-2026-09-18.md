# Android 验证替代路径（2026-09-18）

> 适用票：`CORE-LLAMA-ANDROID-01`
>
> 当前事实：开发机不是 Android 真机；Android 工程只声明 `arm64-v8a`。因此 Gradle 通过、x86_64 模拟器安装成功，都不能单独证明 `libqlh_llama_jni.so` 的 RPC worker 在 ARM64 设备上工作。

## 1. 证据分层

| 层级 | 环境 | 可以证明 | 不能证明 |
| --- | --- | --- | --- |
| A | JVM unit test | 协议、状态机、能力画像、任务 worker 合同 | APK、JNI、真实 socket、热/电 |
| B | Gradle `fullDebug` 构建 | Kotlin/Compose/Room/JNI 编译和 APK 产物 | 运行时 native 加载、设备网络 |
| C | x86_64 Android Emulator | APK 安装、Activity、权限、前后台、HTTP/Tailscale 配置、控制面 | `arm64-v8a` RPC worker；除非 AVD 明确提供 ARM64 ABI 且实际加载 native 库 |
| D | ARM64 AVD / QEMU aarch64 | ARM64 ELF 加载、JNI 路径和基础 RPC 启动兼容性 | 手机热/电、厂商后台策略、真实性能 |
| E | 远程 adb 真机或实体手机 | 真机 native worker、网络、断线、热状态、功耗和长时 | 其他厂商/型号的普遍结论 |

结论：没有真机时先完成 A+B；有 x86_64 模拟器时完成 C；只有 ARM64 AVD/QEMU 时才将 D 记为 ARM64 兼容证据。`CORE-LLAMA-ANDROID-01` 的真机硬门仍保持未完成。

## 2. 开发机命令

从主仓根目录执行：

```powershell
# 默认只跑 Android JVM 单元测试，并打印设备证据状态
python scripts/android_validation.py

# 同时构建 fullDebug APK
python scripts/android_validation.py --assemble

# 有在线模拟器/真机时，安装并启动控制面
python scripts/android_validation.py --assemble --install --launch --serial emulator-5554

# 机器可读结果，适合 CI 或本地证据归档
python scripts/android_validation.py --assemble --json
```

脚本会读取 `adb devices -l` 和 `ro.product.cpu.abi/abilist`，把结果区分为：

- `jvm/build-pass-device-pending`：开发机证据通过，设备尚未验证；
- `device-control-pass-native-worker-pending`：APK 控制面启动通过，但 native worker 仍未达到 ARM64 证据门；
- `native-worker-candidate`：仅表示在线设备是 ARM64 且 APK 已安装启动，仍需 RPC 探活、模型分片、断线和热状态测试，不能直接记为完成。

## 3. 模拟器选择

优先顺序：

1. 普通 x86_64 AVD：用于 C 层控制面验证。由于主 APK 的 native ABI 是 `arm64-v8a`，安装失败或 native library 加载失败是预期边界，不能通过修改结论来伪装成 worker 通过。
2. ARM64 AVD：用于 D 层兼容性验证。需要 Android SDK 提供对应 `system-images`，并接受明显更慢的启动和推理速度。
3. QEMU `aarch64`：用于 JNI/ELF/基础网络兼容性补证，不能用于性能数字。设备镜像、端口转发和运行日志必须作为外部实验资产记录。
4. 远程 adb：将可用 ARM64 设备通过 USB/IP、局域网 adb 或实验室机器接入开发机；脚本只读取设备状态，不把远程设备写入主仓配置。

模拟器或 QEMU 的结果必须明确写出 ABI、Android API level、镜像来源、是否加载 `libqlh_llama_jni.so`、模型工件摘要和网络拓扑。没有这些字段只能算构建/控制面结果。

## 4. P1 验收顺序

1. `:app:testFullDebugUnitTest` 通过。
2. `:app:assembleFullDebug` 通过，检查 APK 中含 `lib/arm64-v8a/libqlh_llama_jni.so`。
3. 在 x86_64 AVD 完成 APK 安装、Activity 启动、权限、前台 Service 和 HTTP 控制面检查；若 native ABI 不匹配，登记为预期限制。
4. 在 ARM64 AVD、QEMU 或远程 ARM64 adb 环境完成 `System.loadLibrary`、RPC worker 状态、端口探活和停止语义检查。
5. 具备实体设备后再做模型分片、弱网断线、热/电、后台回收和长时 soak；这一步不能由模拟器替代。

## 5. 当前边界

- P0 交叉编译、16 KB page 对齐、JNI 接线和能力探测已经有本地证据，证据位于 `local_docs/`，不作为仓库发布资产。
- 当前未将模拟器结果写成真机完成，也未将 APK build 结果写成 RPC worker 完成。
- Android UI 和 Gradle 工程继续属于 `qlh-android` 外置仓；主仓只维护模型/任务/节点能力合同及跨平台 TUI。
